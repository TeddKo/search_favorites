name: core_data
description: Core data package.
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  # Add dependencies here
  core_domain:
    path: ../domain
  shared_util:
    path: ../../shared/util

dev_dependencies:
  test: ^1.26.3
  lint: ^2.8.0
  build_runner: ^2.6.0
